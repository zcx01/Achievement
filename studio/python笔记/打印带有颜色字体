https://www.cnblogs.com/easypython/p/9084426.html
*具体格式*：
print(\033[显示方式;前景色;背景色m)
注：';'前后的三个参数：显示方式，前景色，背景色是可选参数，可以只写其中的某一个或者某两个；由于表示三个参数不同含义的数值都是唯一没有重复的，所以三个参数的书写先后顺序没有固定要求，系统都可识别；
建议按照默认的格式规范书写

注意一般调用玩要修改成默认字体
print('\033[0m')
·