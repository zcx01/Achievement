#pragma once
#include <string>
#include <vector>

class algorithmTest
{
private:
    /* data */
public:
    algorithmTest(/* args */);

    float calAverageValue(float speed);
    std::vector<float> speedSamp;//车速采样 
};

