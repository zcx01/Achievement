摄像机/观察空间
以摄像机的视角为场景中所有的顶点坐标

必须有在世界坐标上的位置、观察的方向、指向它右侧的向量、指向它上方的向量

摄像机位置
定义一个vec3(0.0f,0.0f,30.f)

摄像机方向
用场景原点向量减去摄像机位置向量的结果就是摄像机的指向向量

右轴
先定义一个上向量，在把此向量和第二步得到的方向向量进行叉乘

上轴
把右向量和方向向量进行叉乘

lootAt(QMatrix4x4函数,glm函数)
只要定义一个摄像机位置、一个目标位置、一个表示世界坐标的上向量(计算右轴时的向量)
注*此函数不会重置

